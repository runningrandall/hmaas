import { z } from "zod";
import { extendZodWithOpenApi } from "@asteasolutions/zod-to-openapi";

extendZodWithOpenApi(z);

export const Create{{pascalCase name}}Schema = z.object({
name: z.string().min(1, "Name is required").openapi({ example: "Example" }),
description: z.string().optional().openapi({ example: "A description" }),
}).openapi("Create{{pascalCase name}}");

export type Create{{pascalCase name}}Request = z.infer<typeof Create{{pascalCase name}}Schema>;

    export const {{pascalCase name}}Schema = Create{{pascalCase name}}Schema.extend({
    {{camelCase name}}Id: z.string().openapi({ example: "abc-123" }),
    createdAt: z.number().optional().openapi({ example: 1678900000000 }),
    updatedAt: z.number().optional().openapi({ example: 1678900000000 }),
    }).openapi("{{pascalCase name}}");